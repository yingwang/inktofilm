[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "inktofilm"
version = "0.4.0"
description = "Turn one prompt or screenplay into a tested, finished AI film"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [{name = "Ying Wang"}]
keywords = ["ai-film", "video-generation", "screenplay", "evaluation", "ffmpeg"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.4", "ruff>=0.9"]
fal = ["fal-client>=0.10"]

[project.scripts]
inktofilm = "vidspec.cli:main"
vidspec = "vidspec.cli:main"

[project.urls]
Homepage = "https://github.com/yingwang/inktofilm"
Issues = "https://github.com/yingwang/inktofilm/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "I", "B"]
ignore = ["E501"]
